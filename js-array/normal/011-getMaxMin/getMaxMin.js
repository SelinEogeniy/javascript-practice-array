// getMaxMin

// Функція повинна повернути масив із двох чисел — мінімальне та максимальне значення з масиву.

function getMaxMin(arr) {
  return [Math.min(...arr), Math.max(...arr)];
}

console.log(getMaxMin([4, 1, 9, 2])); // 👉 [1, 9]
