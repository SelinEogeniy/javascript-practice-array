// Створи функцію stringToArray(str), яка приймає рядок і повертає масив слів.

function stringToArray(str) {
  return str.split(" ");
}

// Перевірка:

console.log(stringToArray("JavaScript is cool")); // ["JavaScript", "is", "cool"]
