// Створи функцію mergeArrays(arr1, arr2), яка об’єднує два масиви і повертає результат.

function mergeArrays(arr1, arr2) {
  return arr1.concat(arr2);
}

// Перевірка:

console.log(mergeArrays([1, 2], [3, 4])); // [1, 2, 3, 4]
